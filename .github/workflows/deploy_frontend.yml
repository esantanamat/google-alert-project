name: Frontend Deployment

on:
  push:
    branches:
      - main
    paths:
      - frontend/**
      

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      
     